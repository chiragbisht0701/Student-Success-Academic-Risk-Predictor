Overview

This project is developed as an original project aligned with the concepts covered in the subject syllabus. The project aims to identify a meaningful problem, design a technical solution, implement the solution using the tools and methods learned in the course, and demonstrate understanding through documentation and evaluation.
The project focuses on applying Artificial Intelligence and Machine Learning concepts through a prediction or classification-based solution.

Features

The project includes major functional modules that provide clear input and output and follow a logical workflow of user interaction with the system.
The major functional modules include data input and processing, prediction or classification, and reporting or analytics.
The system follows a structured workflow in which data is provided as input, processed by the system, used for prediction or classification, and the resulting information is presented as output.

Technologies and Tools Used

The project uses appropriate tools and methods related to the subject. The implementation follows the technical expectations of the project, including the use of subject concepts, proper architectural design, modular implementation, documentation, validation, error handling, and Git version control.

Project Requirements

The project includes at least three major functional modules with a clear input/output structure and a logical workflow.
The project also specifies at least four non-functional requirements. These include areas such as performance, usability, reliability, maintainability, error handling, and resource efficiency.

Installation and Running

The project should contain the required project files and configuration files in an organized and modular folder or package structure.
The steps required to install and run the project should be provided in the repository so that the complete project can be executed and demonstrated.

Testing

Testing and validation are included wherever applicable. The project follows the requirement for testing through unit tests or validation tests.
Validation and error handling are also included as part of the technical implementation.
